import { MoveFocusDirective } from './move-focus.directive';

describe('MoveFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new MoveFocusDirective();
    expect(directive).toBeTruthy();
  });
});
